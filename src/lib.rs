// Empty lib target so this package can host uniffi-bindgen / batch_e2e bins
// without publishing a library API. Both bins' sources live in the `shared/`
// submodule; see the `[[bin]]` paths in Cargo.toml.
