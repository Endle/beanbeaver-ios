<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!-- Render with the pre-iOS-26 design language. See CLAUDE.md, "The tab
	     bar opts out of iOS 26's design language" — this is a whole-app flag
	     with an expiry date, not a tab bar setting.

	     Everything else in this app's Info.plist is still generated from
	     INFOPLIST_KEY_* build settings and merged on top of this file; this
	     exists only because UIDesignRequiresCompatibility is not one of the
	     keys that mechanism accepts. -->
	<key>UIDesignRequiresCompatibility</key>
	<true/>
</dict>
</plist>
